# Environment used by the Panther-managed HTTP server during E2E tests
# (PANTHER_APP_ENV=panther in .env.test). Without this file the server would
# fall back to .env and try to reach a real SMTP host, which 500s registration
# in CI where no mailer is reachable.
MAILER_DSN=null://null
