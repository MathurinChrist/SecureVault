{% extends 'base.html.twig' %}

{% block title %}{{ title }} - SecureVault{% endblock %}

{% block body_connected %}
<div class="h-[60vh] flex flex-col items-center justify-center text-center">
    <div class="h-24 w-24 bg-indigo-600/10 rounded-3xl flex items-center justify-center text-4xl mb-6">
        ⏳
    </div>
    <h1 class="text-3xl font-bold mb-2">{{ title }}</h1>
    <p class="text-slate-400">Cette section est en cours de développement.</p>
    <a href="{{ path('app_dashboard') }}" class="mt-8 text-indigo-400 hover:underline">Retour au tableau de bord</a>
</div>
{% endblock body_connected %}
